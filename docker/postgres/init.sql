-- ============================================================
-- LABO.COS App — Initialisation PostgreSQL
-- Active l'extension pg_trgm pour la recherche floue
-- ============================================================

CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE EXTENSION IF NOT EXISTS unaccent;
